{
  "name": "@paperwall/integration-tests",
  "version": "0.1.0",
  "private": true,
  "description": "Integration tests for all Paperwall components",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@types/node": "^25.2.3",
    "@vitest/coverage-v8": "^3.2.4",
    "jsdom": "^28.0.0",
    "vitest": "^3.0.0"
  }
}
